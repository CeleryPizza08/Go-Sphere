import React from 'react'
import styles from '../style'

const Footer = () => (
    <footer className='flex justify-center items-center w-full py-[1vh] bg-primary box-shadow-up'>
        <p className={`${styles.paragraph} text-center text-[16px]`}> RoamRiders building in progress...</p >

    </footer >
)

export default Footer