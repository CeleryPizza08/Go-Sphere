import React from 'react'
import styles from '../style'

const GeoLoc = () => {
    return (
        <div className={`${styles.boxShadow} w-[30vw] h-[50vh] bg-boxlightblue rounded-[10px] border flex items-center justify-center`}>
            <p className='w-full h-full flex items-center justify-center'>GEOLOC API</p>
        </div>
    )
}

export default GeoLoc