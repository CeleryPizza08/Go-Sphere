import React from 'react'
import { SideBar, CreateForm } from '../Components';
import styles from '../style'

const Info = () => (

    <div className={`${styles.paddingX} w-full overflow-hidden flex flex-row items-center justify-center`}>
        <CreateForm />
    </div >

)


export default Info